.idea
.claude/*
# …but do track project skills (shared team tooling), not their install artifacts.
!.claude/skills/

# node / build tooling
node_modules
coverage
# Vite build output — the Worker serves this; CI/deploy regenerate it.
dist
.vite
# generated JUnit report (CI regenerates it each run to publish the test report)
reports

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Playwright E2E artifacts (browser layer writes here when run from repo root)
test-results
