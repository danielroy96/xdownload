.idea
.claude/*
# …but do track project skills (shared team tooling), not their install artifacts.
!.claude/skills/

# node / jest test harness (dev-only; the app ships dependency-free)
node_modules
coverage

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Playwright E2E artifacts (browser layer writes here when run from repo root)
test-results
